package com.example.cp_main_be.domain.avatar.avatar.domain.repository;

import com.example.cp_main_be.domain.avatar.avatar.domain.AvatarMaster;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AvatarMasterRepository extends JpaRepository<AvatarMaster, Long> {}
