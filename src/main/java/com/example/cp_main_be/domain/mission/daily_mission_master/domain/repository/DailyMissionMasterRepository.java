package com.example.cp_main_be.domain.mission.daily_mission_master.domain.repository;

import com.example.cp_main_be.domain.mission.daily_mission_master.domain.DailyMissionMaster;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DailyMissionMasterRepository extends JpaRepository<DailyMissionMaster, Long> {}
